import { Heart } from "lucide-react";import { Link } from "@tanstack/react-router";import { Button } from "@/components/ui/button";import { money,type Product } from "@/data/store";import { useStore } from "@/state/storefront";
export function ProductCard({product}:{product:Product}){const{locale,wishlist,toggleWishlist}=useStore();const liked=wishlist.includes(product.id);return <article className="group min-w-0"><div className="relative overflow-hidden bg-secondary"><Link to="/product/$slug" params={{slug:product.slug}} className="block aspect-[4/5]"><img src={product.images[0]} alt={product.name[locale]} loading="lazy" width={1024} height={1280} className="h-full w-full object-cover transition-transform duration-700 group-hover:scale-[1.025]"/></Link><Button variant="ghost" size="icon" aria-label={liked?"Remove from wishlist":"Add to wishlist"} onClick={()=>toggleWishlist(product.id)} className="absolute right-2 top-2 bg-background/75 hover:bg-background"><Heart className={liked?"fill-primary text-primary":""}/></Button></div><Link to="/product/$slug" params={{slug:product.slug}} className="mt-3 block text-sm transition-colors hover:text-primary">{product.name[locale]}</Link><p className="mt-1 text-xs font-semibold">{money(product.price,locale)}</p></article>}
