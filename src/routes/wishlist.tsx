import { createFileRoute } from "@tanstack/react-router";import { products } from "@/data/store";import { useStore } from "@/state/storefront";import { ProductCard } from "@/components/storefront/ProductCard";
export const Route=createFileRoute("/wishlist")({head:()=>({meta:[{title:"Wishlist — ASSEL KRUSZKA"},{name:"description",content:"Your saved ASSEL KRUSZKA pieces."},{property:"og:title",content:"Wishlist — ASSEL KRUSZKA"},{property:"og:description",content:"Your saved ASSEL KRUSZKA pieces."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Page});function Page(){const{wishlist,locale}=useStore();const list=products.filter(p=>wishlist.includes(p.id));return <main className="page-wrap"><h1 className="font-display text-5xl">{locale==="en"?"Wishlist":"Избранное"}</h1><div className="mt-10 grid grid-cols-2 gap-4 lg:grid-cols-5">{list.map(p=><ProductCard key={p.id} product={p}/>)}</div>{!list.length&&<p className="py-24 text-muted-foreground">Your wishlist is empty.</p>}</main>}
